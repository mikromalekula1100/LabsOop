#pragma once

#include <string>
#include <iostream>
#include "MyArray.h"
// template<typename T> МБМБ
const MyArray& max(const MyArray& firstArray, const MyArray& secondArray);
const MyArray& min(const MyArray& firstArray, const MyArray& secondArray);